include<stdio.h>
void main()
{
	int n,i,sum,temp,r,num1,s,count;
	scanf("%d",&num1);
	scanf("%d",&s);
	
	for(n=s;n<=num1;n++)
    
	{
		sum=0;
		temp=n;
	
    	while(n!=0)
	   {
	    	r=n%10;
		    sum=sum*10+r;
		    n=n/10;
	    }
	    n=temp;
	    if(sum==n)
	    {
		    printf("%d ",n);
		    //i++;
		    //count++;
		    
	    }
    }
	
}
