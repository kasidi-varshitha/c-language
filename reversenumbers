#include<stdio.h>
void main()
{
	int i,n,flag;
	scanf("%d",&n);
	i=1;
	flag=0;
	while(i>=1)
	{
		if(i%2==0)
		{
		
		   if(flag==0)
		   
		   {
		    	printf("%d",i);
			    flag=1;
		   }
		   else
		   {
			printf(",%d",i);
		   }
	    }
	
		i--;
	
    }
}
