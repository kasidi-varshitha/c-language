#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int a;
    scanf("%d",&a);
    int c=a%10;
    if(c>=5)
    {
        printf("%d",a+(10-c));
    }
    else{
        printf("%d",a-c);
    }
    
    return 0;
}
